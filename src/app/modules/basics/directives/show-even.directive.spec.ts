import { ShowEvenDirective } from './show-even.directive';

describe('ShowEvenDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowEvenDirective();
    expect(directive).toBeTruthy();
  });
});
